name: CI Status Workflow

on:
  push:
    branches:
      - main   # Run on every push to main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Simple success step
        run: echo "Workflow runs successfully!"
